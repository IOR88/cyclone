# insallation for scrapy within python
# https://docs.scrapy.org/en/latest/intro/install.html#ubuntu-14-04-or-above
# pip install scrapy
# pip install SQLAlchemy
# pip install beautifulsoup4
# pip install pytest
# pip install psycopg2


# we would run some tests before creating image maybe or before running container ?